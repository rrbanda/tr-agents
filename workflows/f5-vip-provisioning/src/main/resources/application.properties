# F5 VIP Provisioning Workflow - Quarkus Configuration

# F5 BIG-IP iControl REST
quarkus.rest-client.f5-openapi_yaml.url=${F5_HOST:http://f5-mcp-server:8080}

# Connectivity check service
quarkus.rest-client.connectivity-openapi_yaml.url=${CONNECTIVITY_HOST:http://connectivity-mcp-server:8080}

# ServiceNow
quarkus.rest-client.servicenow-openapi_yaml.url=${SERVICENOW_URL:http://servicenow-mcp-server:8080}

# RHDH Notifications
quarkus.rest-client.notifications.url=${NOTIFICATIONS_URL:http://backstage:7007/api/notifications}
