# Branch Outage Response Workflow - Quarkus Configuration

# ServiceNow
quarkus.rest-client.servicenow-openapi_yaml.url=${SERVICENOW_URL:http://servicenow-mcp-server:8080}

# Teams notification service
quarkus.rest-client.teams-openapi_yaml.url=${TEAMS_WEBHOOK_URL:http://notifications-mcp-server:8080}

# SMS alert service
quarkus.rest-client.sms-openapi_yaml.url=${SMS_SERVICE_URL:http://notifications-mcp-server:8080}

# RHDH Notifications
quarkus.rest-client.notifications.url=${NOTIFICATIONS_URL:http://backstage:7007/api/notifications}
